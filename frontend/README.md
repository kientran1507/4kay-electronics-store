# 4Kay Store Frontend

Next.js storefront, customer account, admin dashboard, and AI assistant UI.

Run from the repository root:

```bash
npm run dev:frontend
```

The frontend expects `frontend/.env.local`. See the root
[README](../README.md) and [.env.example](../.env.example).
